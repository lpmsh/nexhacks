# API base URL for the COSMOS backend
# Leave empty if frontend is served by the same server as the API
# Set to the backend URL (e.g., http://localhost:8000) if running frontend separately
VITE_API_BASE=
